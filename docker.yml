# Use postgres/example user/password credentials
version: '3.1'

services:

  mysql:
    image: mysql:5.7
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: ieworforw0owenf
      MYSQL_DATABASE: test-basic
      MYSQL_USER: test
      MYSQL_PASSWORD: test